---
"k-ui-kit": minor
---

Add `hideLabel` to Checkbox. The label leaves the screen and stays in the DOM, so the box keeps its accessible name where its meaning is clear from its surroundings, such as a table row.
