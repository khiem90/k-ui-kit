---
"k-ui-kit": minor
---

Add Switch: a labelled on/off switch on the Radix primitive that announces as a switch, toggles with Space, controlled or uncontrolled, and submits its name and value with the surrounding form. Adds the `--kui-radius-full` Token for pill shapes.
