# Changesets

A pull request that changes shipped code (`src/` other than Stories and docs, `package.json`, or `tsup.config.ts`) adds a file here with `pnpm changeset`. The Release workflow turns the files into a version bump and a CHANGELOG entry. The Release section of the root README has the details.
